#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num = 0;
    printf("++ : %d, --: %d\n", num++, num--);
    // printf("++ : %d, --: %d\n", ++num, --num);
    // printf("++ : %d\n", ++num);
     system("pause");
}
